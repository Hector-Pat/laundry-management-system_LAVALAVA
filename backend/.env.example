PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=lavalavabd

JWT_SECRET=cambia_esta_clave_por_una_segura
JWT_EXPIRES_IN=2h

FRONTEND_URL=http://localhost:5173

# Notificacion por Telegram cuando un pedido pasa a LISTO (RF-04).
# Sin esta variable, el envio se simula con un log marcado como
# "[MOCK TELEGRAM]" en vez de mandar un mensaje real.
# Como obtenerlo: habla con @BotFather en Telegram, usa /newbot y sigue las
# instrucciones; te va a dar un token con forma 123456789:AAExxxxxxxxxxxxx.
TELEGRAM_BOT_TOKEN=
