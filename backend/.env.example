PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=lavalavabd

JWT_SECRET=cambia_esta_clave_por_una_segura
JWT_EXPIRES_IN=2h

# Notificacion por WhatsApp (Twilio) cuando un pedido pasa a LISTO (RF-04).
# Sin estas tres variables, el envio se simula con un log marcado como
# "[MOCK WHATSAPP]" en vez de mandar un mensaje real.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
TWILIO_WHATSAPP_COUNTRY_CODE=+52
